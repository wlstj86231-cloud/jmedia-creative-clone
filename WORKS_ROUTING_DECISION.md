# Works Routing Decision

For this static release, keep the existing detail pages:

- `highlight-info.html`
- `highlight-tool.html`
- `highlight-local.html`
- `highlight-engagement.html`

When the project moves to a templated build step, migrate these into `/works/{slug}/` using the data shape in `project-data.js`.
