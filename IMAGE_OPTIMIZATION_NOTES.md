# J MEDIA Image Optimization Notes

- Portfolio PNG sources are kept as editable originals.
- Matching WebP files are generated in `assets/portfolio/` for deployment optimization.
- The Open Graph image is `assets/og-jmedia.png` at 1200x630.
- Current font strategy uses the local system stack only, so there is no external font request to preload.
