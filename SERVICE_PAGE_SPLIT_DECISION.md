# Service Page Split Decision

The home page keeps the service summary for fast conversion.

A separate service page should be introduced when package details, revision policy, price bands, or delivery examples become too long for the current `#packages` section. Until then, `studio-brief.html` is the deeper trust document and `contact.html` is the action page.
